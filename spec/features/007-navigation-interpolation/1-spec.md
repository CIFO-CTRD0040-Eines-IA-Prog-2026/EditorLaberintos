# 006 — Add navigation interpolation animation

Add interpolation to navigation movement and turning
Keyboard input no loguer executed immediately, it must wait until the current anmation has finished